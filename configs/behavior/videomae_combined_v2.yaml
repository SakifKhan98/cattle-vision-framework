experiment_name: videomae_combined_v2
model_name: MCG-NJU/videomae-base-finetuned-kinetics
num_classes: 7
labels_csv: data/processed/tubelets_rfdetr/labels.csv

train:
  dataset_filter: null
  split_filter: train
  label_subset: null

val:
  dataset_filter: null
  split_filter: val
  label_subset: null

batch_size: 8
grad_accum_steps: 4
num_epochs: 30
lr: 5.0e-5
lr_head: 1.0e-3
weight_decay: 0.05
warmup_epochs: 3
early_stopping_patience: 8
use_class_weights: true

output_dir: runs/behavior/videomae_combined_v2
